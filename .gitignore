# Rust build artifacts
/target

# Cargo.lock is not committed for libraries
Cargo.lock

# Editor / OS junk
*.swp
.DS_Store

# Optional upstream PSD fixture corpus; keep locally, never commit it.
/test/ag-psd/
